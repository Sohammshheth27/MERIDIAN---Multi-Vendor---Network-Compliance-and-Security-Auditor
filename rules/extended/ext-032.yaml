id: MERIDIAN-EXT-032
title: ICMP unreachable rate limiting should be enabled
field: management.icmp_ratelimit
operator: equals
expected: true
severity: low
tier: extended
rationale: >-
  Rate limiting unreachables reduces the device's value as a reconnaissance oracle.
frameworks:
  nist_800_53: ["SC-5"]
  iso_27001_2022: []
  # STIG/CIS ids intentionally omitted: they are per-platform and
  # belong in the vendor pack. An invented id is worse
  # than none.
